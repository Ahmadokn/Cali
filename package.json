{
    "name": "cali",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "postinstall": "npm ci --prefix server && npm ci --prefix client && npm run build --prefix client",
      "start": "node server/server.js"
    },
    "engines": {
      "node": "18.x"
    }
  }
  